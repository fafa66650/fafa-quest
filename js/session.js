import {TEAM_NAMES,ROLE_LIBRARY,DIFFICULTIES} from './data.js';import {storage,publish} from './storage.js';
const uid=(p='q')=>`${p}-${crypto.randomUUID?.()||Math.random().toString(36).slice(2)}-${Date.now().toString(36)}`;function code(){const c='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';return Array.from({length:6},()=>c[Math.floor(Math.random()*c.length)]).join('')}
export function balanceParticipants(names,n){const a=names.map(x=>x.trim()).filter(Boolean);for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}const teams=Array.from({length:n},(_,i)=>({id:uid('team'),name:TEAM_NAMES[i%TEAM_NAMES.length],code:code(),members:[],roles:{}}));a.forEach((x,i)=>teams[i%n].members.push(x));teams.forEach(assignRoles);return teams}export function assignRoles(t){t.roles={};t.members.forEach((m,i)=>t.roles[m]=ROLE_LIBRARY[i%ROLE_LIBRARY.length].id);return t}
export function createSession(o){const diff=DIFFICULTIES.find(d=>d.id===o.difficulty)||DIFFICULTIES[1];const duration=Math.max(10,Math.round((o.durationMin||60)*diff.timeMultiplier));const teams=balanceParticipants(o.participants||[],Math.max(1,o.teamCount||1));const s={id:uid('session'),title:o.title||'Partie FAFA QUEST',scenarioId:o.scenarioId,difficulty:o.difficulty||'aventure',durationMin:duration,baseDurationMin:o.durationMin||60,playMode:o.playMode||'central',competition:o.competition||'classement',createdAt:Date.now(),startedAt:null,pausedAt:null,pausedTotalMs:0,teams,version:3};storage.set('sessions:'+s.id,s);storage.set('lastSessionId',s.id);publish('session:create',{sessionId:s.id});return s}
export const getSession=id=>storage.get('sessions:'+id);export function saveSession(s){storage.set('sessions:'+s.id,s);publish('session:update',{sessionId:s.id});return s}export function allSessions(){return storage.keys().filter(k=>k.startsWith('sessions:')).map(k=>storage.get(k)).filter(Boolean).sort((a,b)=>b.createdAt-a.createdAt)}
export function findTeamByCode(c){const code=String(c||'').trim().toUpperCase();for(const s of allSessions()){const t=s.teams.find(x=>x.code===code);if(t)return {session:s,team:t}}return null}
export function renameTeam(session,teamId,name){const t=session.teams.find(x=>x.id===teamId);if(t&&name.trim())t.name=name.trim().slice(0,30);return saveSession(session)}
